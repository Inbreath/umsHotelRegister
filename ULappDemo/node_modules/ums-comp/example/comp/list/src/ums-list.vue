<template>
  <list>
    <cell v-for="(item, i) in items" :key="i" append="tree">
      <ums-list-item :title="item.title" :url="item.url"></ums-list-item>
    </cell>
  </list>
</template>

<script>
  var getBaseURL = require('./base-url.js').getBaseURL
  export default {
    props: {
      dir: {
        default: 'examples'
      }, // examples, test ...
      items: {
        default: [
          {name: 'ums-list', title: 'Hello World', url: ''}
        ]
      }
    },
    components: {
      umsListItem: require('./ums-list-item.vue')
    },
    created: function() {
      var base = getBaseURL(this)
      for (var i in this.items) {
        var item = this.items[i];
        if (!item.url) {
          item.url = base + item.name + '.js';
        }
      }
    }
  }
</script>
